# UI Style Museum

一個展示各種 UI 設計風格的互動式博物館，使用 Vue 3 + TypeScript 構建。

最初為 Gemini Canvas 產生，後來使用 AI 將 React 版本轉換為 Vue 版本。

## 指令

```bash
# 安裝依賴
yarn

# 開發
yarn dev

# 構建
yarn build

# 預覽
yarn preview

# 型別檢查
yarn type-check
```
